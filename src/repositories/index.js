const subscriptionRepository = require("./subscription");
const userRepository = require("./user");
const repositoryRepository = require("./repository");

module.exports = {
  subscriptionRepository,
  userRepository,
  repositoryRepository,
};
